{
  "name": "slack-scheduler-backend",
  "version": "1.0.0",
  "description": "Slack Message Scheduler Backend",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "python3 -m gunicorn core.wsgi:application --bind 0.0.0.0:$PORT"
  },
  "cacheDirectories": [
    "node_modules"
  ]
}